[phases.setup]
# Nixpacks auto-detects Node.js from package.json
nixPkgs = ["nodejs", "python3", "git"]

[build]
cmds = ["pnpm run build:node"]

[start]
cmd = "pnpm run dockerstart"

[cache]
paths = [
  "/root/.pnpm-store",
  "node_modules",
  ".next",
  "build"

]